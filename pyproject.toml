[project]
name = "kagent"
version = "0.1.1"
description = "A tool-calling LLM agent wrapper based on Ame-chan from Needy Streamer Overload."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "toffeezzz" }]
dependencies = [
  "openai>=2.54.0,<3.0.0",
  "rich",
  "pydantic",
  "send2trash",
  "python-dotenv",
  "prompt-toolkit",
  "markdown",
  "pyqt6",
  "PyMuPDF",
  "frontmatter",
]

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
kagent = "main:main"

[project.entry-points."pygments.styles"]
toffeezzz-pink = "modules.gui.toffeezzz_pink:ToffeezzzPink"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
